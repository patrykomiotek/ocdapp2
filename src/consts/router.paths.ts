export enum Paths {
  HOME = "/",
  LOGIN = "/login",
  PRODUCTS = "/products",
  PRODUCT_DETAILS = "/products/:id",
  CREATE_PRODUCT = "/products/create",
  BASKET = "/basket",
  STEPPER = "/stepper",
  AUTH = "/auth",
  ACCOUNT = "/account",
}
